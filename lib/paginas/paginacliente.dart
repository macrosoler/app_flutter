import 'package:flutter/material.dart';

// ignore: camel_case_types
class PaginaCliente extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Clientes', textScaleFactor: 2.0),
    );
  }
}
